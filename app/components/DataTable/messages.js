/*
 * DataTable Messages
 *
 * This contains all the text for the DataTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DataTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DataTable component!',
  },
});
