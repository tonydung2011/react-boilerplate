/*
 * DotaItemsAll Messages
 *
 * This contains all the text for the DotaItemsAll container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DotaItemsAll';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'ALL DOTA2 TRADE ITEMS',
  },
  error: {
    id: `${scope}.header`,
    defaultMessage: 'Network error',
  },
});
